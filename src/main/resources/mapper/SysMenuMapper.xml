<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gong.mapper.SysMenuMapper">


    <select id="selectById" resultType="SysMenu">
        SELECT id, parent_id, menu_name, path, component, menu_type, cache, status, icon, power, create_by, update_by, create_time, update_time
            FROM sys_menu
                WHERE id = #{id} AND status = 1;
    </select>

    <select id="selectByPower" resultType="SysMenu">
        SELECT id, parent_id, menu_name, path, component, menu_type, cache, status, icon, power, create_by, update_by, create_time, update_time
            FROM sys_menu
                WHERE power >= #{power} AND status = 1
    </select>


</mapper>